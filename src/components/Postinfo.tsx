'use client';
import CustomImage from './CustomImage';

const Postinfo = () => {
  return (
    <div className="cursor-pointer size-4 relative">
      <CustomImage src="icons/infoMore.svg" alt="" w={16} h={16} />
    </div>
  );
};

export default Postinfo;
